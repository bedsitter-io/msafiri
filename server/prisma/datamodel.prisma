type Safari {
  id: ID! @unique
  createdAt: DateTime! @createdAt
  title: String!
  description: String!
  postedBy: User
}

type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  safaris: [Safari!]!
}